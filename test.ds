@8 a = 5 + 2;
@8 b = 7 + 4;
if (a+1 == b-3);
{
	.mov byte[rbp-32], 'h';
	.mov byte[rbp-31], 'e';
	.mov byte[rbp-30], 'l';
	.mov byte[rbp-29], 'l';
	.mov byte[rbp-28], 'o';
	.mov byte[rbp-27], 0;
	.mov rax, 1;
	.mov rdi, 1;
	.sub rbp, 32;
	.mov rsi, rbp;
	.add rbp, 32;
	.mov rdx, 5;
	.syscall;
};
