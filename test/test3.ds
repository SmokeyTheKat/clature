sub @8 ddPrint_cstring(@8 strconst)
{
	@8 strlen = getLength(strconst);
	.mov	rax, 1;
	.mov	rdi, 1;
	.mov	rsi, [rbp+16];
	.mov	rdx, [rbp-8];
	.syscall;
	
}
sub @8 printNum(@8 num)
{
	num += 48;
	.mov	rax, 1;
	.mov	rdi, 1;
	.add	rbp, 16;
	.mov	rsi, rbp;
	.sub	rbp, 16;
	.mov	rdx, 1;
	.syscall;
}
sub @8 getLength(@8 str)
{
	@8 output = 0;
	while @1[str+output] != 0
	{
		output++;
	}
	return output;
}
sub @8 printChar(@8 chr)
{
	.mov	rax, 1;
	.mov	rdi, 1;
	.add	rbp, 16;
	.mov	rsi, rbp;
	.sub	rbp, 16;
	.mov	rdx, 1;
	.syscall;
}
@8 text_length = "the length is: ";
@1 endl = 10;

iso printChar(endl);
iso printChar(endl);


@8 hello = "hello";
iso ddPrint_cstring(hello);

iso printChar(endl);

@8 helloLen = getLength(hello);
iso ddPrint_cstring(text_length);
iso printNum(helloLen);

iso printChar(endl);

@8 str = "yo bro what is going on yo";
iso ddPrint_cstring(str);
iso printChar(endl);
@1 chr = 0;
@8 i = 0;
while @1[str+i] != 0
{
	chr = @1[str+i];
	printChar(chr);
	i++;
}
@8 empty = 4;
