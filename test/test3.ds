sub @8 printChar(@8 chr)
{
	.mov	rax, 1;
	.mov	rdi, 1;
	.add	rbp, 16;
	.mov	rsi, rbp;
	.sub	rbp, 16;
	.mov	rdx, 1;
	.syscall;
}
@8 str = "yo bro what is going on yo";
@1 chr = 0;
@8 i = 0;
while @1[str+i] != 0
{
	chr = @1[str+i];
	printChar(chr);
	i++;
}
@8 empty = 10;
